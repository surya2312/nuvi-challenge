import { platformBrowserDynamic } from '@angular/platform-browser-dynamic';
import {NgbModule} from '@ng-bootstrap/ng-bootstrap';
import { NuviApp } from './app.module';

platformBrowserDynamic().bootstrapModule(NuviApp);
